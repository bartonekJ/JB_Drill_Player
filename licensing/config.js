// Shipping-safe default. Store builds will replace this file during packaging.
// Keeping licensing disabled here preserves unrestricted development builds.
window.__JB_DRILL_LICENSE_CONFIG__ ||= Object.freeze({
  enabled: false,
  testMode: false,
  endpoint: "",
  leasePublicKey: null,
});
